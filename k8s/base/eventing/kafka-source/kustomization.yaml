apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: kafka-source-eventing
  annotations:
    config.kubernetes.io/local-config: "true"

resources:
  # Will contain:
  # - kafka-source.yaml

# Placeholder - resources will be added in Task 6
